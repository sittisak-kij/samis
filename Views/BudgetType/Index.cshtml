@model IEnumerable<samis.Models.BudgetType>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.budgetTypeName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.budgetTypeName)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.budgetTypeId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.budgetTypeId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.budgetTypeId">Delete</a>
                </td>
            </tr>
            }
        </tbody>
    </table>